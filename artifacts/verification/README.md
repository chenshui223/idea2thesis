# Verification Evidence

This directory stores repository-level verification evidence for the `idea2thesis` codebase itself.

It is separate from per-job generated artifacts under `jobs/`.

Files here should capture the exact commands and outcomes used to verify the repository before sharing or publishing it.
